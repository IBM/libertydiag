<?xml version="1.0" encoding="UTF-8"?>
<server description="new server">

    <!-- Enable features -->
    <featureManager>
        <feature>jakartaee-9.1</feature>
        <feature>microProfile-5.0</feature>
    </featureManager>

    <!-- Best practice: generate an encoded password using bin/securityUtility encode. -->
    <keyStore password="password" />

    <!-- Best practice: generate an encoded password using bin/securityUtility encode. -->
    <basicRegistry id="basic" realm="BasicRealm">
        <user name="admin" password="password" />
        <group name="Administrators">
            <member name="admin" />
        </group>
    </basicRegistry>
    <administrator-role>
        <group>Administrators</group>
    </administrator-role>

    <!-- Decide whether administrator authentication (administrator-role) is required for /metrics.
         HTTPS may still be used without authentication. -->
    <mpMetrics authentication="false" />

    <!-- By default, mpMetrics enables all monitor-1.0 metrics. A space for a filter disables
         these metrics and just leaves base and app metrics which has less performance overhead.
         If needed, enable additional metrics by replacing the space with a command-delimited list from:
         JVM,ThreadPool,WebContainer,Session,ConnectionPool,REST -->
    <monitor filter=" " />

    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080"
                  httpsPort="9443" />

    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true"/>

    <!-- Configures the application on a specified context root -->
    <webApplication contextRoot="/" location="libertydiag.war" />

    <!-- Default SSL configuration enables trust for default certificates from the Java runtime -->
    <ssl id="defaultSSLConfig" trustDefaultCerts="true" />
</server>
